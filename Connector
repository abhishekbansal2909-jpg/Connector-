from prophet.plot import plot_plotly, plot_components_plotly

# Generate the interactive Prophet forecast chart
fig = plot_plotly(prophet_model, forecast)

# Enhance the visual layout for readability
fig.update_layout(
    title=dict(
        text='Rico Auto (RICOAUTO.NS) - Interactive Forecast',
        x=0.5,
        font=dict(size=18)
    ),
    xaxis_title='Date',
    yaxis_title='Stock Price (INR)',
    hovermode='x unified', # Groups the hover data together on a single vertical line
    template='plotly_white'
)

# Display the interactive chart
fig.show()
